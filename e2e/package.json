{
  "name": "reagir-information-e2e",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "test": "cypress open",
    "test:ci": "cypress run --config-file cypress.ci.json",
    "start": "docker-compose -f ../docker-compose.yml -f docker-compose.e2e.yml",
    "start:local": "yarn start -f ../docker-compose.override.yml up --abort-on-container-exit postgresql api restoredb app cypress",
    "start:ci": "yarn start -f docker-compose.ci.yml up --abort-on-container-exit"
  },
  "devDependencies": {
    "@cypress/code-coverage": "^3.7.6",
    "nyc": "^15.1.0",
    "wait-on": "^5.0.0"
  }
}
