export { rootReducer } from './reducers';
export { Store, GetState, Dispatch, Dependencies } from './types';
export { AppState } from './AppState';
export { setComment, setCommentsArea } from './normalize';
